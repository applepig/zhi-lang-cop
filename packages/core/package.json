{
  "name": "zhi-lang-cop-core",
  "version": "0.2.0",
  "description": "Core textlint rule for detecting PRC Chinese terms",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "zhi-lang-cop": "./dist/cli.js"
  },
  "keywords": ["textlint", "textlint-rule", "chinese", "taiwan"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config vitest.e2e.config.ts"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "execa": "^8.0.1"
  },
  "files": ["dist", "../data"]
}
